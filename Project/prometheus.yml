global:
  scrape_interval: 5s # Collect metrics every 5 seconds

scrape_configs:
  - job_name: 'photo-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['photo-app:8081'] # Target the photo-app container inside the docker network
